int* InsertSort(int* masIS, int n);
int* quicksort(int* masQS, int first, int last);
void Merge(int* A, int first, int last);
void MergeSort(int* A, int first, int last);
int* BoubleSort(int* A, int n);